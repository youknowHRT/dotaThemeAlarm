<view class='container' wx:if="{{visible}}">
  <view class='confirm'>
    <!-- <textarea placeholder="{{placeholder}}" value="{{value}}" bindinput="watchValue">
    </textarea> -->
    <view class='messageTap'>{{placeholder}}</view>
    <view class='actions'>
      <view hover-class="active" class='sure' bindtap="confirm">确定</view>
      <view hover-class="active" bindtap="cancel">{{backText}}</view>
    </view>
  </view>
  
</view>